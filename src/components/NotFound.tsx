import { Typography } from "@mui/material";

const NotFound = () => {
  return (
    <Typography variant="h2" sx={{ m: 5 }}>
      Page not found. Please try starting over.
    </Typography>
  );
};

export default NotFound;
